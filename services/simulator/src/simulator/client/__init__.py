"""Generated shapes (`models.py`) plus the thin, hand-written call layer
(`api.py`) that never encodes anything the generator didn't already type —
the same split as `apps/web`'s `openapi-typescript` + `openapi-fetch`."""
